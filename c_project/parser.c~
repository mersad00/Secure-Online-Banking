#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<errno.h>

int main(int argc, char ** args)
{
    if(argc < 2)
    {
        printf("add file argument");
        exit(-1);
    }
    if(argc > 2)
    {
        printf("too many arguments");
    }
    printf("hello\n");
    
    return 0;
}
